{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "clipboard-popup",
  "description": "Capabilities for the standalone clipboard popup window",
  "windows": ["clipboard-popup"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}
